{
  "name": "bear-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "build": "bun build src/server.ts --outfile dist/server.js --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "e2e": "bun run scripts/e2e-bear.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.4.0"
  }
}
